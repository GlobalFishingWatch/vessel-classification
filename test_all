#!/bin/bash

set -e

# Run scala dataflow tests.
pushd scala
./sbt test
popd

# Run python TF tests.
pushd python
python classification/utility_test.py
popd

# Build docker containers
docker build -f deploy/inference/Dockerfile .
